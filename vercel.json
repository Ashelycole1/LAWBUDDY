{
  "buildCommand": "flutter/bin/flutter build web --release --dart-define=GEMINI_API_KEY=$GEMINI_API_KEY --dart-define=GROQ_API_KEY=$GROQ_API_KEY --dart-define=OPENROUTER_API_KEY=$OPENROUTER_API_KEY",
  "outputDirectory": "build/web",
  "installCommand": "if ! [ -d flutter ]; then git clone https://github.com/flutter/flutter.git --depth 1 -b stable && flutter/bin/flutter config --enable-web; fi && flutter/bin/flutter pub get",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
